package com.yaumi.app.ui.theme

import androidx.compose.ui.graphics.Color

// Night sky — used for hero gradient & PrayerHeroCard
val IndigoNight = Color(0xFF1B1145)
val RoyalPurple = Color(0xFF3D2B9E)
val SoftViolet = Color(0xFF6B4FCB)

// Lavenders — page background, surface borders
val Lavender50 = Color(0xFFF5F3FF)
val Lavender100 = Color(0xFFEDE8FF)
val Lavender200 = Color(0xFFD8CEF5)

// Gold lantern — accent, countdown, daily verse highlight
val GoldCrescent = Color(0xFFF5C518)
val GoldSoft = Color(0xFFFFE08A)
val GoldDeep = Color(0xFFB8860B)
val CreamManuscript = Color(0xFFFFF8E6)

// Ink — text scale on light surfaces
val Ink900 = Color(0xFF1A1033)
val Ink700 = Color(0xFF3D2B6E)
val Ink500 = Color(0xFF6B5FA3)
val Ink300 = Color(0xFF9B8FCC)

// Surfaces
val SurfaceWhite = Color(0xFFFFFFFF)
val SurfaceSoft = Color(0xFFFBF8FC)

// Tile tints — Jelajahi grid
val TintQuran = Color(0xFFEDE8FF)
val TintHadis = Color(0xFFE8F4EE)
val TintDoa = Color(0xFFFFE8F0)
val TintKiblat = Color(0xFFFFF4E8)
val TintTasbih = Color(0xFFE8F4FF)
val TintHijriah = Color(0xFFF0FFE8)

// On-dark text helpers (for hero / prayer card)
val OnNightHigh = Color(0xCCFFFFFF) // 80%
val OnNightMid = Color(0x99FFFFFF)  // 60%
val OnNightLow = Color(0x66FFFFFF)  // 40%
val OnNightLine = Color(0x26FFFFFF) // 15%
